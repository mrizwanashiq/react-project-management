import styled from "styled-components"

export const LogoWrapper = styled.div`
	margin-top: 30px;
	margin-bottom: 60px;
	&:hover {
		cursor: pointer;
	}
	svg {
		display: block;
		max-width: 100%;
	}
`
